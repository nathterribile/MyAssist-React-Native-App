import styled from 'styled-components/native';

const card = styled.card`
  margin-bottom: 16px;
  align-items: center;
`;

const cardImage = styled.cardImage`
  width: 150px;
  height: 150px;
  margin-bottom: 8px;
`;

const styles = {
  card, cardImage,
};

export default styles;
